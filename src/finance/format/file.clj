(ns finance.format.file
  "Plain files in a tree structure representing the dataset."
  (:require
    [clojure.java.io :as io]
    [clojure.string :as str]))


(defn read-data
  "Load a collection of entity data from the given directory of files."
  [root-dir]
  ;; TODO: implement
  ,,,)


(defn write-data!
  "Writes a collection of entity data to a tree of files rooted in the provided
  directory."
  [root-dir data]
  ;; TODO: implement
  ,,,)
